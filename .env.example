# Copy this file to .env.local. Never commit real secrets.
# Required before saving provider keys from Personal Center. Use a stable random value of 32+ characters.
AI_KEYS_ENCRYPTION_KEY=replace_with_a_random_secret_at_least_32_chars

# Optional legacy fallback. New installations can configure DeepSeek in Personal Center instead.
DEEPSEEK_API_KEY=
